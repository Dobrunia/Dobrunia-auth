# URL API auth (тот же хост/порт, что слушает сервер). Origin этого URL должен совпадать с проверкой return_url OAuth на клиенте.
# На сервере: тот же API-домен + в CORS должны быть origin вашего SPA (см. AUTH_WEB_PUBLIC_URL и CORS_ORIGINS / CORS_REFLECT_ORIGIN).
VITE_API_URL=http://localhost:3000
