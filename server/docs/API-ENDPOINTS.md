# Endpoints

Краткое описание публичных HTTP-эндпоинтов сервера (как сейчас реализовано в коде). Базовый URL в разработке: `http://localhost:<PORT>` (порт из `PORT` в `.env`, по умолчанию `3000`).

Ошибки с известным статусом отдаются как JSON: `{ "error": { "message": "..." } }`. Неожиданные ошибки — `500` с тем же видом и текстом `Internal Server Error` (без деталей стека в теле).

### CORS (браузерный фронт на другом origin)

- **`CORS_ORIGINS`**: разрешённые значения заголовка `Origin` через запятую, например `http://localhost:5173,https://auth.example.com`. По умолчанию: `http://localhost:5173` (локальный Vite).
- Если в списке есть **`*`**, выставляется `Access-Control-Allow-Origin: *` (удобно только для отладки).
- Preflight **`OPTIONS`** — ответ **`204`** и те же правила заголовков. Разрешены методы `GET`, `POST`, `DELETE` и заголовки `Authorization`, `Content-Type`.

---

## `GET /health`

### Flow

- **Зачем:** проверка, что процесс жив (оркестраторы, балансеры, CI).
- **Что делается:** контроллер вызывает сервис без обращения к БД; возвращается фиксированный `status: ok` и текущий `timestamp` в ISO.
- **Ответ:** `200`, тело `{ "status": "ok", "timestamp": "<ISO-8601>" }`.

---

## `POST /auth/register`

### Flow

- **Зачем:** создать глобального пользователя в auth-сервисе и сразу выдать сессию + токены для указанного **клиента** (приложения-потребителя), чтобы приложение могло считать пользователя «вошедшим».
- **Тело (JSON):** `email`, `password`, и обязательно один из идентификаторов клиента: `clientId` или `clientSlug` (строка может быть UUID клиента или slug, например `dobrunia-auth-web`). Email нормализуется (trim + lower case). Длина пароля задаётся константами в `validation.constants`.
- **Из запроса:** для сессии сохраняются `ip` (с учётом `trust proxy` и `X-Forwarded-For`) и `User-Agent`.
- **Валидация:** Zod; при ошибке — `400`.
- **Бизнес-логика (транзакция):**
  - резолв активного клиента по `clientId` / `clientSlug`;
  - проверка, что email ещё не занят;
  - хеш пароля (argon2id);
  - вставка пользователя;
  - создание сессии со статусом `active`, привязка к `user_id` и `client_id`;
  - генерация **opaque** refresh-токена, в БД пишется только **SHA-256** от него;
  - выпуск **access** JWT (HS256, срок из `ACCESS_TOKEN_EXPIRES_SEC`).
- **Успех:** `201`, тело: `user` (`id`, `email`), `session` (`id`, `clientId`, `clientSlug`), `accessToken`, `refreshToken` (сырой refresh только здесь; в БД — хеш).
- **Ошибки:** `400` (невалидное тело по Zod; клиент не найден или неактивен — сообщение `Unknown or inactive client`), `409` (email уже зарегистрирован или дубликат на уровне БД).

---

## `POST /auth/login`

### Flow

- **Зачем:** войти существующим пользователем в контексте конкретного клиента; выдать **новую** сессию и пару токенов (как при регистрации).
- **Тело:** то же, что у register (`email`, `password`, `clientId` или `clientSlug`).
- **Из запроса:** IP и User-Agent пишутся в новую сессию.
- **Валидация:** Zod → при ошибке `400`.
- **Бизнес-логика (транзакция):**
  - резолв активного клиента;
  - загрузка пользователя по email с полями для проверки пароля и `is_active`;
  - отказ без детализации, если: пользователя нет, аккаунт неактивен, нет `password_hash` (например, только OAuth), пароль не совпал — везде одно сообщение для клиента;
  - обновление `last_login_at` у пользователя;
  - новая сессия `active`, новый refresh (в БД — хеш), новый access JWT.
- **Успех:** `200`, формат тела как у register (`user`, `session`, `accessToken`, `refreshToken`).
- **Ошибки:** `400` (только ошибки Zod-валидации тела). Неизвестный/неактивный клиент, неверные учётные данные, неактивный пользователь, отсутствие пароля в БД — всё это отдаётся как **`401`** с текстом `Invalid email or password` (намеренно одно сообщение). Редкий конфликт на уровне БД при вставке — `409` `Session conflict`.

---

## `POST /auth/logout`

### Flow

- **Зачем:** завершить **текущую** сессию, связанную с выданным refresh-токеном (одно устройство / один вход).
- **Тело (JSON):** `refreshToken` — тот же непрозрачный токен, что вернули register/login (не хеш).
- **Логика (транзакция):**
  - считается SHA-256 от переданного refresh;
  - ищется **активная** запись в `refresh_tokens` (не отозвана, не просрочена);
  - если найдена — помечается отзыв refresh с причиной `logout`, сессия переводится в `revoked` с той же причиной;
  - если не найдена (неверный токен, истёк, уже logout) — **ничего не ломаем**: идемпотентное поведение.
- **Успех / «тихий» случай:** `204`, **пустое тело** (клиент всё равно удаляет токены локально).
- **Ошибки:** `400`, если тело не прошло валидацию (например, нет или пустой `refreshToken`).

---

## `POST /auth/refresh`

### Flow

- **Зачем:** продлить авторизацию без повторного логина, когда истёк короткий access JWT: клиент отправляет действующий **opaque** refresh, получает новый access и новый refresh (ротация).
- **Тело (JSON):** `refreshToken` — тот же формат, что вернули register/login (не хеш).
- **Логика (транзакция):**
  - SHA-256 от переданного refresh;
  - поиск **активной** записи в `refresh_tokens` (не отозвана, не просрочена), с привязкой к пользователю и email для payload access JWT;
  - проверка, что связанная **сессия** существует и в статусе `active` для этого `user_id`;
  - вставка **новой** строки refresh (та же сессия и пользователь, тот же `family_id` или он берётся от старого токена, если был `NULL`; `previous_token_id` — старый id);
  - отзыв старого refresh: `revoked_at`, причина `rotated`, `replaced_by_token_id` → новый id;
  - выпуск нового access JWT (`sub` = user id, `sid` = session id, `email`).
- **Успех:** `200`, тело `{ "accessToken": "<jwt>", "refreshToken": "<новый opaque>" }`. Старый refresh после успеха считать недействительным.
- **Ошибки:** `400` (Zod: нет/пустой `refreshToken`). Любая несогласованность (неизвестный/отозванный refresh, неактивная сессия и т.п.) — **`401`** `Invalid or expired refresh token` (намеренно одно сообщение, без детализации).

---

## `GET /auth/me`

### Flow

- **Зачем:** узнать текущего пользователя и контекст сессии/клиента по действующему access JWT (после логина, F5, межсервисные вызовы с передачей Bearer).
- **Заголовок:** `Authorization: Bearer <accessToken>` (обязателен валидный неистёкший JWT из этого сервиса).
- **Логика:** проверка подписи и срока access, извлечение `sub` (user id) и `sid` (session id); один запрос в БД: активная сессия с этим id и пользователем, join `users` и `clients`.
- **Успех:** `200`, тело:
  - `user`: `id`, `email`, `firstName`, `lastName`, `avatarUrl` (безопасный профиль);
  - `session`: `id`, `clientId`, `clientSlug`, `clientName`.
- **Ошибки:** нет заголовка / не `Bearer` — **`401`** `Authorization Bearer token required`. Невалидный или истёкший JWT, либо сессия не найдена/не `active` — **`401`** `Invalid or expired access token`.

---

## `GET /sessions`

### Flow

- **Зачем:** дашборд «где я авторизован» — все сессии текущего пользователя по всем клиентам (браузеры, устройства, проекты).
- **Заголовок:** `Authorization: Bearer <accessToken>` (как у `/auth/me`).
- **Логика:** по `sub` из JWT выбираются все строки `sessions` этого пользователя, join `clients` для `slug` и `name`.
- **Успех:** `200`, тело `{ "sessions": [ ... ] }`. Элементы: `id`, `status` (`active` / `revoked` / …), `clientId`, `clientSlug`, `clientName`, `ipAddress`, `userAgent`, `lastSeenAt`, `createdAt`, `revokedAt`, `revokeReason` (даты в ISO 8601, nullable — как в БД).
- **Ошибки:** **`401`** при отсутствии/невалидном JWT (те же тексты, что у access-защищённых роутов).

---

## `DELETE /sessions/:id`

### Flow

- **Зачем:** завершить одну конкретную сессию (одно устройство / один проект), не трогая остальные.
- **Заголовок:** `Authorization: Bearer <accessToken>`.
- **Параметр:** `id` — UUID сессии.
- **Логика (транзакция):** сессия должна принадлежать пользователю из JWT (`user_id = sub`); иначе **`404`** `Session not found`. Иначе: отзыв **всех** ещё не отозванных `refresh_tokens` этой сессии (причина `session_revoked`), затем перевод сессии в `revoked` с той же причиной (как при logout, только для одной сессии). Уже отозванная сессия: refresh всё равно чистятся, `UPDATE` сессии может затронуть 0 строк — ответ всё равно успешный.
- **Успех:** **`204`**, пустое тело.
- **Ошибки:** **`401`** JWT; **`404`** чужая или несуществующая сессия.

---

## `GET /clients/:id/sessions`

### Flow

- **Зачем:** те же сессии пользователя, но только для одного приложения-клиента (группировка в UI по проекту).
- **Заголовок:** `Authorization: Bearer <accessToken>`.
- **Параметр пути:** `id` — UUID клиента **или** `slug` (как в login/register).
- **Логика:** резолв активного клиента; если не найден — **`404`** `Client not found`. Иначе список сессий текущего пользователя с `client_id` этого клиента (тот же формат элементов, что у `GET /sessions`).
- **Успех:** `200`, `{ "sessions": [ ... ] }`.
- **Ошибки:** **`401`** JWT; **`404`** неизвестный/неактивный клиент.

---

## `GET /oauth/authorize`

### Flow

- **Зачем:** старт OAuth2 **authorization code** flow для внешнего проекта: пользователь перенаправляется на auth-сервис с параметрами клиента и `redirect_uri`.
- **Query (обязательные):** `client_id` (UUID или `slug` клиента), `redirect_uri` (точное совпадение с одним из значений в `clients.oauth_redirect_uris`), `response_type` — только значение `code`.
- **Query (опционально):** `state` — произвольная строка клиента (CSRF); возвращается в редиректе на `redirect_uri` без изменений.
- **Проверки:** клиент активен; для клиента задан непустой JSON-массив разрешённых `redirect_uri`; переданный `redirect_uri` **точно** совпадает с одним из них.
- **Уже вошёл:** если есть httpOnly-кука `dobrunia_oauth` (JWT после успешного `POST /oauth/authorize`) и сессия в БД всё ещё `active` для этого же `client_id`, выдаётся одноразовый `code` и выполняется **302** на `redirect_uri?code=...&state=...` (параметры добавляются к URL callback).
- **Иначе:** **200** `text/html` — простая страница входа (форма `POST /oauth/authorize` с теми же полями в hidden + email/password).
- **Ошибки конфигурации / параметров:** **400** HTML-страница с пояснением (нельзя безопасно редиректить на неизвестный `redirect_uri`).

---

## `POST /oauth/authorize`

### Flow

- **Зачем:** отправка логина с hosted-страницы OAuth; тело обычно `application/x-www-form-urlencoded` (поля формы).
- **Поля:** те же, что в query GET (`client_id`, `redirect_uri`, `response_type`, опционально `state`), плюс `email`, `password`.
- **Логика:** те же проверки клиента и `redirect_uri`, что у GET; далее та же цепочка, что `POST /auth/login` для данного клиента (новая сессия, в БД создаётся сессия + refresh — как при API-логине).
- **Успех:** **302** на `GET /oauth/authorize?...` (те же параметры), заголовок **`Set-Cookie`**: кука браузерной OAuth-сессии (`dobrunia_oauth`, httpOnly, `SameSite=Lax`, в production добавляется `Secure`). Второй заход по редиректу на GET обычно сразу выдаёт `code` и уводит на приложение.
- **Неверный пароль:** **401** HTML с формой и общим текстом ошибки (без детализации).
- **Остальное:** **400** HTML при невалидных параметрах; прочие ошибки — по общим правилам (например **409** через middleware, если сработает защита от дубликатов).

---

## `POST /oauth/token`

### Flow

- **Зачем:** обмен одноразового `code` на токены **сервером** приложения (не из браузера пользователя, если следуете best practice — вызывать с backend).
- **Тело:** JSON или `application/x-www-form-urlencoded` с полями: `grant_type` = `authorization_code`, `code` (значение из query на callback), `redirect_uri` (тот же, что при authorize), `client_id` (UUID или slug, как при authorize).
- **Логика (транзакция):** резолв клиента; блокировка строки кода (`FOR UPDATE`); проверка `client_id`, точного `redirect_uri`, срока, `used_at IS NULL`; сессия по-прежнему `active`; вставка **новой** строки refresh для той же сессии; пометка кода использованным; commit; выпуск access JWT.
- **Успех:** **200** JSON в стиле login/register: `user` (`id`, `email`), `session` (`id`, `clientId`, `clientSlug`), `accessToken`, `refreshToken`.
- **Ошибки:** **400** при ошибке Zod или при любом несоответствии кода/клиента/redirect/сессии — одно сообщение `Invalid authorization request` (без уточнения причины). Редкий **409** `Token conflict` при коллизии на уровне БД.

### Данные в БД

- У клиента поле **`oauth_redirect_uris`** (JSON-массив строк) задаётся миграциями/админкой; без него authorize вернёт ошибку «OAuth не настроен».
- Коды хранятся в **`oauth_authorization_codes`** (в БД только **SHA-256** от `code`, как у refresh).

---

## Связка с архитектурой продукта

- **Клиент** в БД — это приложение экосистемы; без `clientId`/`clientSlug` нельзя однозначно привязать сессию к проекту. Для OAuth в браузере список **`oauth_redirect_uris`** ограничивает, куда можно вернуть пользователя с `code`.
- **Сессия** хранится в auth-сервисе; downstream-приложение после обмена токенов может поднять свою серверную сессию отдельно.
- **Refresh** никогда не хранится в открытом виде в БД — только хеш; подтверждение владения на logout — снова хеш переданного значения.
